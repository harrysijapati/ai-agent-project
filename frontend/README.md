# AI Agent Frontend

## Setup

1. Install dependencies:
   ```bash
   npm install
   ```

2. Start the development server:
   ```bash
   npm run dev
   ```

The frontend will run on http://localhost:3000

Make sure the backend is running on port 3001 before using the frontend.
